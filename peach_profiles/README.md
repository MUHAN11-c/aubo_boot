# 桃子采摘参数档案

档案由 Web 任务中心保存，文件写入采用临时文件、`fsync` 和原子替换。名称只允许字母、
数字、点、下划线和连字符。交付档案故意不默认打开机械臂、抓取或工具 IO；现场完成低速
验证并人工上电后，再通过 Web 二次确认显式应用策略。

参数分级：运动周期运行中禁止修改 `execution/grasp/tool`；三级依赖固定为
`execution → grasp → tool`。任何档案都不能上电、松刹车或恢复急停/防护停。

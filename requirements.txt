# aubo_py3.12 venv 依赖约束（Python 3.12，--system-site-packages）
#
# 用法：
#   aubo_py3.12/bin/pip install -r requirements.txt
#
# 关键约束：numpy 必须 <2。该 venv 带 --system-site-packages，ROS jazzy 的
# cv_bridge 以及 apt 的 python3-opencv / python3-scipy 均按 numpy 1.x 编译；
# venv 内若出现 numpy 2.x，import cv2 报 "numpy.core.multiarray failed to
# import"，import cv_bridge 直接段错误。
#
# 以下由系统 site-packages 提供，切勿在本文件中 pip 安装同名包（会 shadow
# 系统版）：scipy、cv_bridge、rclpy、pytest、PyYAML（系统已有）。
# 例外：ultralytics 会拉取 opencv-python；本机已验证与 numpy==1.26.4 /
# cv_bridge 共存。若出现段错误，可尝试：
#   aubo_py3.12/bin/pip uninstall -y opencv-python opencv-python-headless
# 并依赖 apt 的 python3-opencv。
#
# 版本锁定基准：2026-07-30 本机 aubo_py3.12 实测。

# ---- 核心（tools / diagnostics / 全项目 Python） ----
numpy==1.26.4
matplotlib==3.11.1

# ---- peach_pose_ros2（YOLO + MobileSAM via ultralytics） ----
torch==2.13.0
torchvision==0.28.0
ultralytics==8.3.0
ultralytics-thop==2.1.5

# ---- aubo_scene_recon（Open3D 点云/TSDF） ----
open3d==0.19.0

# ---- matplotlib 传递依赖（保证绘图环境可复现） ----
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

# aubo_py3.12 venv 依赖约束（Python 3.12，--system-site-packages）
#
# 用法：
#   aubo_py3.12/bin/pip install -r requirements.txt
#
# 关键约束：numpy 必须 <2。该 venv 带 --system-site-packages，ROS jazzy 的
# cv_bridge 以及 apt 的 python3-opencv / python3-scipy 均按 numpy 1.x 编译；
# venv 内若出现 numpy 2.x，import cv2 报 "numpy.core.multiarray failed to
# import"，import cv_bridge 直接段错误。
#
# 以下包由系统 site-packages 提供，切勿在 venv 内 pip 安装同名包（会 shadow
# 系统版并破坏 numpy 1.x 二进制兼容）：cv2(opencv)、scipy、cv_bridge、rclpy、
# pytest、yaml。

# ---- 直接依赖（当前环境已验证） ----
numpy==1.26.4        # 1.26.x 末版，支持 py3.12，与 ROS jazzy 二进制栈兼容
matplotlib==3.11.1   # tools/ 与 diagnostics/ 绘图；已验证与 numpy 1.26.4 兼容

# ---- 传递依赖锁定（matplotlib 依赖树，保证重建环境可复现） ----
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

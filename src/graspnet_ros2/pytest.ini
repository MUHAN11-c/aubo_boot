[pytest]
testpaths = test
# vendored 推理子集依赖 venv 内 torch（系统 python 没有），禁止 pytest 收集深入；
# 其正确性由 test_grasp_core.py 的 torch 守卫用例与推理冒烟覆盖。
norecursedirs =
    .*
    build
    dist
    graspnet_ros2/graspnet_lib

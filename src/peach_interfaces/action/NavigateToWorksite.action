# 走到作业位。服务端 peach_navigation；唯一客户端 peach_task_executor。
# navigation_enabled=false（默认）时调度不发送本动作，批次直接 BeginScene。
# 现行服务端是 reserved_stub（固定座视为已到位）。发行版 Nav2 实现预留。

geometry_msgs/PoseStamped pose
string site_id
---
bool arrived
string failure_code
---
float32 progress
string message

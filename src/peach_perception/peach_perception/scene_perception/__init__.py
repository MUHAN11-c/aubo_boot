"""
场景感知算法与节点（`peach_scene_perception_node`）.

`scene_perception_node.py` 是 ROS 外壳。其余模块为零 ROS 纯核：检测/分割/拟合/身份/锁定。
通用原语在 `peach_perception.common`。

离线评估：``python -m peach_perception.scene_perception.offline.e2e_validate``
"""

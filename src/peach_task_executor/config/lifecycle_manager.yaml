peach_lifecycle_manager:
  ros__parameters:
    # 有序 configure → activate；拆除逆序。observability 不进名单。
    node_names:
      - peach_scene_perception_node
      - peach_target_reconstruction_node
      - peach_manipulation_skills_node
      - peach_navigation_node
      - peach_task_executor
    startup_timeout_s: 60.0

# aubo_boot 部署编译运行全流程

## 环境要求

| 项 | 版本 |
|------|------|
| 操作系统 | Ubuntu 22.04 |
| ROS2 | Humble |
| Python | 3.10（系统自带） |
| GPU | NVIDIA + CUDA Toolkit 13.x |
| 编译器 | build-essential, ninja-build |

## 核心决策

- **不使用 Conda**：ROS2 节点 shebang `#!/usr/bin/python3` 固定调用系统 Python，Conda 替换 Python 导致 rclpy 不兼容
- **不使用 venv**：同理，ROS2 节点不会使用 venv 的 Python
- **使用 `pip3 install --user`**：包装到 `~/.local/lib/python3.10/site-packages/`，系统 Python 自动识别，ROS2 官方推荐

---

## 一、目录结构

```
~/aubo_boot/
├── deploy.sh           # 环境部署脚本
├── build.sh            # GraspNet 编译脚本
├── activate.sh         # 环境激活脚本
├── aubo_ros2_ws/       # ROS2 工作空间
│   ├── src/            # 源码
│   │   ├── graspnet_ros2/graspnet-baseline/  # GraspNet 核心
│   │   │   ├── pointnet2/   # CUDA 扩展
│   │   │   ├── knn/         # CUDA 扩展
│   │   │   └── graspnetAPI/ # GraspNet API
│   │   └── ...
│   ├── build/          # colcon 构建产物
│   ├── install/        # colcon 安装产物
│   └── start_aubo_new_driver.sh  # 一键启动
└── 部署编译运行全流程.md
```

---

## 二、部署脚本 `deploy.sh`

### 执行内容（5 步）

```
[1] 系统包        apt 安装 ROS2 + 工具链 + 基础工具
[2] CUDA Toolkit  有 GPU 则安装 nvcc 编译器
[3] rosdep        安装 ROS2 包依赖
[4] Python 依赖    pip3 install --user 安装所有 Python 包
[5] 激活脚本      生成 activate.sh
```

### Python 包清单

```
核心 Web：    httpx, websockets, Flask, fastapi, uvicorn, pydantic
视觉/ML：     numpy==1.23.4, opencv-python, scipy, Pillow, PyYAML,
              trimesh, tqdm, matplotlib
GPU 推理：    torch, torchvision, torchaudio (CUDA 13.0)
              open3d, ultralytics, onnxruntime-gpu
构建基础：    packaging>=23, setuptools<80,>=70
```

### 关键修复

1. **python3-fastapi 冲突**：系统 apt 的 fastapi 0.63 与 venv pydantic 2.x 不兼容。部署脚本已排除系统的 `python3-fastapi`，全部走 pip

2. **packaging 版本**：系统 `python3-packaging` 版本太旧（21.3），导致 setuptools 调用 `canonicalize_version(strip_trailing_zero=False)` 失败。pip 安装新版 packaging>=23 解决

3. **setuptools 版本**：setuptools 82.x 有破坏性变更，锁定 <80

### 幂等性

所有步骤均支持重复执行：
- 系统包：`dpkg -l` 逐个检查，已装则跳过
- CUDA Toolkit：检查 `nvcc` 是否存在
- Python 包：pip 自身支持增量，已装包自动跳过

---

## 三、编译脚本 `build.sh`

### 编译内容

```
→ graspnet-baseline + graspnetAPI    pip install -e（editable，源码修改即时生效）
→ pointnet2                          pip install --no-build-isolation -e（CUDA 编译）
→ knn                                pip install --no-build-isolation -e（CUDA 编译）
→ 验证                               导入链完整性检查
```

### 关键修复

1. **`--no-build-isolation`**：pointnet2/knn 的 setup.py 中 `import torch`，pip 隔离构建环境没有 torch，必须去隔离

2. **`-fPIC` 与 nvcc 13.x**：CUDA 13.3 的 nvcc 不认 `-fPIC` 参数，修改 pointnet2/setup.py：
   ```python
   "cxx":  ["-O2", "-fPIC", "-I..."],           # C++ 文件用 -fPIC
   "nvcc": ["-O2", "-Xcompiler", "-fPIC", "-I..."],  # CUDA 文件用 -Xcompiler -fPIC
   ```

3. **导入顺序污染**：`import models`（editable）会把源码目录加到 `sys.path`，其中的 `graspnetAPI/` 子目录污染同名包。修复：源码中将 `from graspnetAPI import GraspGroup` 移到 `import models` 之前

4. **pyproject.toml**：pointnet2/knn 是旧式 setup.py，新版 pip 不支持其 editable 安装。补充最小 `pyproject.toml`：
   ```toml
   [build-system]
   requires = ["setuptools"]
   build-backend = "setuptools.build_meta"
   ```

### 验证输出示例

```
→ 验证 ...
  pointnet2._ext:  .../pointnet2/pointnet2/_ext.cpython-310-x86_64-linux-gnu.so
  knn_pytorch:     .../knn/knn_pytorch/knn_pytorch.cpython-310-x86_64-linux-gnu.so
  ✅ 全部通过
```

---

## 四、激活脚本 `activate.sh`

```bash
source /opt/ros/humble/setup.bash                         # ROS2 环境
source ~/aubo_boot/aubo_ros2_ws/install/setup.bash        # 工作空间
export PATH="/usr/local/cuda/bin:$HOME/.local/bin:$PATH"  # CUDA + pip user bin
```

---

## 五、启动

### 一键启动

```bash
source ~/aubo_boot/activate.sh
cd ~/aubo_boot/aubo_ros2_ws
./start_aubo_new_driver.sh
```

### 跳过构建

```bash
SKIP_BUILD=1 ./start_aubo_new_driver.sh
```

### 启动流程（自动）

| 步骤 | 内容 | 依赖 |
|------|------|------|
| [0] | colcon build | — |
| [1] | 机械臂核心（MoveIt2 + Dashboard + JTC） | build |
| [2] | Demo Driver 服务 | move_group |
| [3-5] | 相机栈 | — |
| [6-7] | 视觉栈（手眼标定 + VPE） | 相机 |
| [8] | GraspNet 点云 | 相机 + 手眼 TF |
| [9-12] | Worker 组（抓取/快换/拉花） | 核心服务 |
| [15] | Web 网关（FastAPI + rosbridge + foxglove） | build |

### 访问地址

| 面板 | URL |
|------|-----|
| 门户首页 | http://127.0.0.1:8090/index.html |
| 视觉抓取 | http://127.0.0.1:8090/vision_grasp_panel.html |
| 咖啡拉花 | http://127.0.0.1:8090/coffee_latte_panel.html |

### 停止

关闭 terminator 窗口，或 `Ctrl+C` 主脚本。脚本已通过 `trap cleanup` 自动清理所有子进程。

---

## 六、已知无害警告

| 警告 | 来源 | 处理 |
|------|------|------|
| `Failed to import ros dependencies in rigid_transforms.py` | autolab_core（graspnetAPI 依赖）检测 ROS1 环境失败 | 无视，ROS2 功能正常 |
| `autolab_core not installed as catkin package` | 同上 | 无视 |
| `Not found any device!` | 相机未连接 | 仿真模式正常 |
| `/recognize_objects not available` | MoveIt 可选功能 | 无视 |
| `Could not enable FIFO RT scheduling` | 非 root 用户权限限制 | 无视 |
| `Old-style arguments are deprecated` | TF 发布节点参数格式 | 无视 |

---

## 七、故障排查速查表

| 现象 | 原因 | 解决 |
|------|------|------|
| `ModuleNotFoundError: No module named 'torch'` | PYTHONPATH 未设置 | 检查 `~/.local` 在 PATH 中 |
| `cannot import name 'GraspGroup' from 'graspnetAPI' (unknown location)` | 导入顺序污染 | graspnetAPI 必须在 models 之前导入 |
| `nvcc fatal: Unknown option '-fPIC'` | CUDA 13.x 不认 -fPIC | 改用 `-Xcompiler -fPIC` |
| `canonicalize_version() got unexpected keyword argument` | packaging 版本太旧 | `pip install --user 'packaging>=23'` |
| `missing 'build_editable' hook` | setuptools 太新 | 锁定 `setuptools<80` |
| `ModuleNotFoundError: No module named 'torch'` during pip install | 隔离构建环境无 torch | 加 `--no-build-isolation` |

---

## 八、Git SSH 免密配置

```bash
# 1. 生成密钥
ssh-keygen -t ed25519 -N "" -f ~/.ssh/id_ed25519

# 2. 复制公钥，粘贴到 https://github.com/settings/keys → New SSH Key
cat ~/.ssh/id_ed25519.pub

# 3. 添加 GitHub 主机密钥
ssh-keyscan -H github.com >> ~/.ssh/known_hosts
```

---

## 九、全新机器部署清单

```bash
# 1. 克隆仓库（SSH）
git clone git@github.com:MUHAN11-c/aubo_boot.git ~/aubo_boot
cd ~/aubo_boot
git checkout dev

# 2. 部署环境
bash deploy.sh

# 3. 编译
bash build.sh

# 4. 启动
source activate.sh
cd aubo_ros2_ws && ./start_aubo_new_driver.sh
```

---

*最后更新: 2026-06-26*
*机器: wjz-vpc, Ubuntu 22.04, RTX 5070 Ti, CUDA 13.3*
